class RepositoryConstants {
  RepositoryConstants._();

  // FireStore constants -----------------------------------------------------------------------
  static const userCollectionPath = "users";
  static const userDistanceCollectionPath = "user_distance";
}